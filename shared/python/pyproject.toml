[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "enterprise-kb"
version = "0.1.0"
description = "Internal runtime support for enterprise knowledge base services"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["enterprise_kb"]
